# P1 Sprint Session — 2026-03-03

## Summary
Six issues closed (#871, #873, #875, #876, #877, #878) across 5 PRs (#891–#896).  
Final state: **1,430 tests, 0 failures**.

## Work Completed

### Barton
- **#871: Wired 5 Affix Properties**
  - Implemented: `EnemyDefReduction`, `HolyDamageVsUndead`, `BlockChanceBonus`, `ReviveCooldownBonus`, `PeriodicDmgBonus`
  - Added run-level flag `PhoenixExtraChargeUsed` on Player for revive cooldown bonus
  - All properties integrated into combat damage calculation and turn mechanics

### Hill
- **#873: Normalized JsonDerivedType Discriminators**
  - Standardized all `[JsonDerivedType]` discriminator strings to all-lowercase
  - Rule: class name lowercased with no separators (e.g., `DarkKnight` → `"darkknight"`)
  - Prevents silent deserialization failures from mixed casing
  - Backward compatibility note: old PascalCase saves will not deserialize; no migration tooling added (saves ephemeral in dev phase)

### Romanoff
- **#875: Added DisplayService Smoke Tests**
  - Established `AnsiConsole.Console` swap pattern as team standard
  - Added 8 tests to HelpDisplayRegressionTests (test count: 1422 → 1430)
  - Covered: `ShowInventory`, `ShowEquipment`, `ShowSkillTreeMenu`, `ShowHelp`, `ShowCombatStatus` (variants)
  - All tests use `[Collection("console-output")]` to prevent parallel execution races

### Fitz
- **#876, #877, #878: CI Improvements**
  - Added osx-x64 release target (cross-compilation via .NET 10)
  - Raised Stryker threshold: `--threshold-break` 50 → 65, `--threshold-low` 65 → 75
  - Verified 80.01% coverage satisfies #878 (78% floor requirement)
  - Note: First Monday Stryker run will validate mutation score; threshold may need adjustment if failed

### Coordinator (fix)
- **Loot Table Static State Contamination**
  - Added `ResetTierPools()` to clean static tier pool state between tests
  - Fixed `EmptyTierPools` test cleanup
  - Ensures no test isolation issues in loot system

## Key Decisions

1. **All 5 affix properties retained** (not pruned) — clean integration into combat system
2. **Lowercase discriminators for JSON polymorphism** — eliminates case-sensitivity bugs
3. **AnsiConsole swap pattern standardized** — use `[Collection("console-output")]` to prevent races
4. **Stryker threshold raised** — 65 is target; adjusted if Monday run fails
5. **No save migration for discriminator changes** — ephemeral dev saves

## Test Status
- **Count:** 1,430 tests
- **Failures:** 0
- **Coverage:** 80.01% (satisfies #878)

## PRs Merged
- #891 (Barton: affix wiring)
- #892 (Hill: discriminator casing)
- #893 (Romanoff: smoke tests)
- #894–#896 (Fitz/Coordinator: CI + loot state fixes)
